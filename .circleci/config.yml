version: 2
jobs:
  build:
    docker:
    - image: python:3.6-stretch
    steps:
    - run: mkdir /tmp/artifacts
    - run: date >> /tmp/artifacts/index.html
    - store_artifacts:
        path: /tmp/artifacts
workflows:
  version: 2
  build_and_test:
    jobs:
    - build
